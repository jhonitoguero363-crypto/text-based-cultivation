---
description: 玩法/数值设定变更必须同步更新 docs
alwaysApply: true
---

# 设定与文档同步

本仓库以 `docs/` 为设定权威说明；`src/constants` 为实现数据源。

## 必须遵守

1. **凡新增或调整玩法、数值、规则、身份、掉落、设施行为**，同一轮改动内必须更新对应 `docs/*.md`，不得只改代码。
2. **页面不展示的细则表**（如刑期对照、亲密增量、入门阈值等）写在文档中，勿堆在 UI。
3. 改常量文件时，优先同步：
   - `docs/玩法系统.md` — 历练、拜访、切磋、双修、受伤、思过崖、洞府等玩法
   - `docs/宗门设定.md` — 宗门等级、入门身份、修为晋升、月俸、设施
   - `docs/境界与修炼.md` — 境界、突破、灵根、修炼速度
   - 专项文档（功法/法术/人物/任务/秘境/丹药等）按主题更新
4. 涉及目录/名录批量数据时，可跑对应 `scripts/export-*-docs.mjs` 或 `gen-docs.mjs`，并检查手写规则段落是否仍正确。
5. 在 `docs/README.md` 的「数据源」中补充新增的关键常量文件路径。

## 自检

完成设定向改动后确认：

- [ ] 相关 docs 已反映最新阈值 / 概率 / 流程
- [ ] 与旧表述冲突的句子已删除或改写
- [ ] 跨文档引用（如思过崖刑期表）仍指向正确位置
